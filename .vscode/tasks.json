{
    "tasks": [
        /*
        {
            "type": "cppbuild",
            "label": "C/C++: cl.exe build active file",
            "command": "cl.exe",
            "args": [
                "/Zi",
                "/EHsc",
                "/nologo",
                "/Fe${fileDirname}\\${fileBasenameNoExtension}.exe",
                "${file}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$msCompile"
            ],
            "group": {
                "kind": "build",
                "isDefault": false
            },
            "detail": "Task generated by Debugger."
        },
        */
        {
            "type": "shell",
            "label": "Rose Compile",
            "command": "rose.compile",
            "args": [
                "${file}",
                "-v",
                "-o", ".build\\${fileBasenameNoExtension}.dll",
            ],
            "options": {
                "cwd": "${fileDirname}\\.."
            },
            "group": {
                "kind": "build",
            }
        },

        {
            "type": "shell",
            "label": "Rose Clean",
            "command": "echo",
            "args": [
                "Hello",
            ],
            "options": {
                "cwd": "${fileDirname}\\.."
            },
            "group": {
                "kind": "build",
            }
        },

        {
            "type": "shell",
            "label": "Rose Build",
            "command": "echo",
            "args": [
                "Build",
            ],
            "options": {
                "cwd": "${fileDirname}\\.."
            },
            "dependsOn": ["Rose Compile", "Rose Clean"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },


    ],
    "version": "2.0.0"
}